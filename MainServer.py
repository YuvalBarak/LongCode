from Classes import *
import time
import threading as thread

def Server(ip, port):
    ThreadsList = []
    ThreadsLock = myThread.Lock()



if __name__ == "__main__":
    ip='0.0.0.0'
    port=45
    Server(ip, port)
